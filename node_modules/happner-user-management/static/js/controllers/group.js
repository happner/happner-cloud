happner_user_management.controller('group', 
	['$scope', 
	'$modal', 
	'happnerAngularClient',
	function($scope, $modal, happnerAngularClient) {
			
		$scope.control = {header:'groups'};	

		$scope.searched = {};

		$scope.searched.items = [
			{name:'admin', network:'network', permissions:['*'], description:'the default admin group for this mesh'}
		];

		$scope.searchFilter = '';

		$scope.searchItems = function(){

			console.log('searching', $scope.searchFilter);

			happnerAngularClient.getClient().exchange.happnerSecurityGroup.list({name:$scope.searchFilter}, function(e, groups){

				if (e) return; //TODO to what? to woo?

				$scope.searched.items = groups;
				$scope.$apply();
			});

		}

		$scope.createItem = function(){

			console.log('create item', name);
		}

		$scope.deleteItem = function(id){

			console.log('delete item', id);
		}

		$scope.editItem = function(item){

			console.log('edit item', item);
		}
	}]
);
